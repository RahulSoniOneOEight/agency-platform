name: agency_widgetbook
description: Internal component and pattern review surface for the agency design system.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  agency_flutter_ui:
    path: ../../packages/agency_flutter_ui
  widgetbook: ^3.25.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
