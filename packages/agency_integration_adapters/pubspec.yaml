name: agency_integration_adapters
description: Deterministic, provider-neutral fake ERP/payment/shipping/CRM/WhatsApp adapters.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  agency_production_core:
    path: ../agency_production_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
