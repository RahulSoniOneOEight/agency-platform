name: agency_supabase_adapter
description: Supabase-backed implementations of the provider-neutral production ports.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  agency_production_core:
    path: ../agency_production_core
  supabase_flutter: ^2.17.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
