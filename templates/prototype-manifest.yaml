version: 1
client_id: example-client
runtime: apps/prototype_app
default_direction: a
directions:
  a: directions/direction-a.yaml
  b: directions/direction-b.yaml
  c: directions/direction-c.yaml
fixture_pack: prototype/fixtures/demo.yaml
theme:
  preset: premium-modern
review:
  query_parameter: direction
  allowed_values: [a, b, c]
